<div class="container">
  <h3 class="p-md-4">注文履歴詳細</h3>
  <div class="row">
    <div class="col-md-10 offset-md-1">
      
        <div class="row py-3">
          <table class="col-7">
            <tbody>
              <tr>
                <th>購入者</th>
                <td><%= @order.customer.name %></td>
              </tr>
              <tr>
                <th>注文日</th>
                <td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
              </tr>
              <tr>
                <th>配送先</th>
                <td>
                  <%= @order.postal_code %>
                  <%= @order.address %></br>
                  <%= @order.name %>
                </td>
              </tr>
              <tr>
                <th>支払方法</th>
                <td><%= @order.payment_method_i18n %></td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="row justify-content-between pt-5">
          <table class="table col-8 px-2">
            <thead>
              <tr>
                <th>商品名</th>
                <th>単価(税込)</th>
                <th>数量</th>
                <th>小計</th>
              </tr>
            </thead>
            <% @order_details.each do |order_detail| %>
            <tbody>
              <tr>
                <td><%= order_detail.item.name %></td>
                <td><%= order_detail.price %></td>
                <td><%= order_detail.amount %></td>
                <td><%= order_detail.subtotal %></td>
              </tr>
            </tbody>
            <% @total += order_detail.subtotal %>
            <% end %>
          </table>
          
          <table class="col-2 pl-2">
            <tbody>
              <tr></tr>
              <tr>
                <th>送料</th>
                <th><%= @order.shipping_charge %></th>
              </tr>
              <tr>
                <th>商品合計</th>
                <th><%= @total %></th>
              </tr>
              <tr>
                <th>請求金額</th>
                <th><%= @order.total_amount %></th>
              </tr>
            </tbody>
          </table>
          
        </div>
    </div>
  </div>
</div>