check

  <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>商品名</th>
            <th>単価(税込)</th>
            <th>数量</th>
            <th>小計</th>
          </tr>
        </thead>
        <% @check_orders.each do |order| %>
          <tbody>
            <tr>
              <td>
                <%= image_tag order.item.get_sweets_image(100, 100) %>
                <%= order.item.name %>
              </td>
              <td><%= order.item.with_tax_price %></td>
              <td><%= order.amount %></td>
              <td><%= order.subtotal %></td>
            </tr>
          </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>

